# improc-petrsu-websharp
A web app written in C# using ASP .NET Core that uses the main improc-petrsu library written in Rust

To build, you need:
- a working rust installation (with cargo)
- cargo-nuget
- .NET Core
